// @ts-nocheck

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: identity/v1/identity.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "identity.v1";

export interface OAuthLoginRequest {
  /** / The identity provider (e.g., 'google', 'apple', 'facebook'). */
  provider: string;
  /** / The OIDC identity token or access token provided by the client's login flow. */
  idToken: string;
}

export interface RegisterLocalRequest {
  email: string;
  password: string;
}

export interface LoginLocalRequest {
  email: string;
  password: string;
}

export interface AuthResponse {
  /** / The UUIDv7 representing the user's unique YourApp identifier. Empty if validation fails. */
  userId: string;
  /** / True if the credentials or token were verified successfully. */
  valid: boolean;
}

export interface UpdateLocalEmailRequest {
  /** / The unique UUIDv7 identifier of the user. */
  userId: string;
  /** / The new email address to bind to the local credential record. */
  newEmail: string;
}

export interface UpdateLocalEmailResponse {
  /** / True if the email update was committed successfully. */
  success: boolean;
}

function createBaseOAuthLoginRequest(): OAuthLoginRequest {
  return { provider: "", idToken: "" };
}

export const OAuthLoginRequest: MessageFns<OAuthLoginRequest> = {
  encode(message: OAuthLoginRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.provider !== "") {
      writer.uint32(10).string(message.provider);
    }
    if (message.idToken !== "") {
      writer.uint32(18).string(message.idToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OAuthLoginRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOAuthLoginRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.idToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OAuthLoginRequest {
    return {
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
      idToken: isSet(object.idToken)
        ? globalThis.String(object.idToken)
        : isSet(object.id_token)
        ? globalThis.String(object.id_token)
        : "",
    };
  },

  toJSON(message: OAuthLoginRequest): unknown {
    const obj: any = {};
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    if (message.idToken !== "") {
      obj.idToken = message.idToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OAuthLoginRequest>, I>>(base?: I): OAuthLoginRequest {
    return OAuthLoginRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OAuthLoginRequest>, I>>(object: I): OAuthLoginRequest {
    const message = createBaseOAuthLoginRequest();
    message.provider = object.provider ?? "";
    message.idToken = object.idToken ?? "";
    return message;
  },
};

function createBaseRegisterLocalRequest(): RegisterLocalRequest {
  return { email: "", password: "" };
}

export const RegisterLocalRequest: MessageFns<RegisterLocalRequest> = {
  encode(message: RegisterLocalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterLocalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterLocalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterLocalRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: RegisterLocalRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterLocalRequest>, I>>(base?: I): RegisterLocalRequest {
    return RegisterLocalRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterLocalRequest>, I>>(object: I): RegisterLocalRequest {
    const message = createBaseRegisterLocalRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseLoginLocalRequest(): LoginLocalRequest {
  return { email: "", password: "" };
}

export const LoginLocalRequest: MessageFns<LoginLocalRequest> = {
  encode(message: LoginLocalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoginLocalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoginLocalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoginLocalRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: LoginLocalRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoginLocalRequest>, I>>(base?: I): LoginLocalRequest {
    return LoginLocalRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoginLocalRequest>, I>>(object: I): LoginLocalRequest {
    const message = createBaseLoginLocalRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseAuthResponse(): AuthResponse {
  return { userId: "", valid: false };
}

export const AuthResponse: MessageFns<AuthResponse> = {
  encode(message: AuthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.valid !== false) {
      writer.uint32(16).bool(message.valid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthResponse {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
    };
  },

  toJSON(message: AuthResponse): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthResponse>, I>>(base?: I): AuthResponse {
    return AuthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthResponse>, I>>(object: I): AuthResponse {
    const message = createBaseAuthResponse();
    message.userId = object.userId ?? "";
    message.valid = object.valid ?? false;
    return message;
  },
};

function createBaseUpdateLocalEmailRequest(): UpdateLocalEmailRequest {
  return { userId: "", newEmail: "" };
}

export const UpdateLocalEmailRequest: MessageFns<UpdateLocalEmailRequest> = {
  encode(message: UpdateLocalEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.newEmail !== "") {
      writer.uint32(18).string(message.newEmail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateLocalEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateLocalEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newEmail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateLocalEmailRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      newEmail: isSet(object.newEmail)
        ? globalThis.String(object.newEmail)
        : isSet(object.new_email)
        ? globalThis.String(object.new_email)
        : "",
    };
  },

  toJSON(message: UpdateLocalEmailRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.newEmail !== "") {
      obj.newEmail = message.newEmail;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateLocalEmailRequest>, I>>(base?: I): UpdateLocalEmailRequest {
    return UpdateLocalEmailRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateLocalEmailRequest>, I>>(object: I): UpdateLocalEmailRequest {
    const message = createBaseUpdateLocalEmailRequest();
    message.userId = object.userId ?? "";
    message.newEmail = object.newEmail ?? "";
    return message;
  },
};

function createBaseUpdateLocalEmailResponse(): UpdateLocalEmailResponse {
  return { success: false };
}

export const UpdateLocalEmailResponse: MessageFns<UpdateLocalEmailResponse> = {
  encode(message: UpdateLocalEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateLocalEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateLocalEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateLocalEmailResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: UpdateLocalEmailResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateLocalEmailResponse>, I>>(base?: I): UpdateLocalEmailResponse {
    return UpdateLocalEmailResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateLocalEmailResponse>, I>>(object: I): UpdateLocalEmailResponse {
    const message = createBaseUpdateLocalEmailResponse();
    message.success = object.success ?? false;
    return message;
  },
};

/**
 * / Manages the core identity of a user, including automated onboarding, OAuth
 * / verification, and traditional email/password fallback.
 */
export interface IdentityService {
  /** / Authenticates a user via an external OAuth provider (creating them if new). */
  OAuthLogin(request: OAuthLoginRequest): Promise<AuthResponse>;
  /** / Provisions a new user using a traditional email and password. */
  RegisterLocal(request: RegisterLocalRequest): Promise<AuthResponse>;
  /** / Verifies a user's plaintext password against their stored Argon2id hash. */
  LoginLocal(request: LoginLocalRequest): Promise<AuthResponse>;
  /** / Updates the email address associated with local credentials. */
  UpdateLocalEmail(request: UpdateLocalEmailRequest): Promise<UpdateLocalEmailResponse>;
}

export const IdentityServiceServiceName = "identity.v1.IdentityService";
export class IdentityServiceClientImpl implements IdentityService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || IdentityServiceServiceName;
    this.rpc = rpc;
    this.OAuthLogin = this.OAuthLogin.bind(this);
    this.RegisterLocal = this.RegisterLocal.bind(this);
    this.LoginLocal = this.LoginLocal.bind(this);
    this.UpdateLocalEmail = this.UpdateLocalEmail.bind(this);
  }
  OAuthLogin(request: OAuthLoginRequest): Promise<AuthResponse> {
    const data = OAuthLoginRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "OAuthLogin", data);
    return promise.then((data) => AuthResponse.decode(new BinaryReader(data)));
  }

  RegisterLocal(request: RegisterLocalRequest): Promise<AuthResponse> {
    const data = RegisterLocalRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RegisterLocal", data);
    return promise.then((data) => AuthResponse.decode(new BinaryReader(data)));
  }

  LoginLocal(request: LoginLocalRequest): Promise<AuthResponse> {
    const data = LoginLocalRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "LoginLocal", data);
    return promise.then((data) => AuthResponse.decode(new BinaryReader(data)));
  }

  UpdateLocalEmail(request: UpdateLocalEmailRequest): Promise<UpdateLocalEmailResponse> {
    const data = UpdateLocalEmailRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "UpdateLocalEmail", data);
    return promise.then((data) => UpdateLocalEmailResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
