// @ts-nocheck

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: human_verification/v1/human_verification.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "human_verification.v1";

export interface GetChallengeRequest {
  /** / The target verification provider identifier (e.g., 'turnstile', 'recaptcha', 'minigame'). */
  providerId: string;
  /** / Optional specifier for tracking specific challenge layouts, versions, or difficulty tiers. */
  editionId: string;
}

export interface GetChallengeResponse {
  /** / A raw JSON string containing the payload the frontend needs to render the challenge widget. */
  challengePayload: string;
}

export interface VerifyRequest {
  /** / The target verification provider identifier that processed the challenge response. */
  providerId: string;
  /** / The raw JSON string or token sent back by the client widget after completion. */
  clientPayload: string;
}

export interface VerifyResponse {
  /** / True if the user successfully completed the challenge. */
  success: boolean;
  /** / A cryptographic, time-limited voucher token proving verification success. Empty if verification fails. */
  voucher: string;
}

function createBaseGetChallengeRequest(): GetChallengeRequest {
  return { providerId: "", editionId: "" };
}

export const GetChallengeRequest: MessageFns<GetChallengeRequest> = {
  encode(message: GetChallengeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.providerId !== "") {
      writer.uint32(10).string(message.providerId);
    }
    if (message.editionId !== "") {
      writer.uint32(18).string(message.editionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetChallengeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChallengeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.editionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChallengeRequest {
    return {
      providerId: isSet(object.providerId)
        ? globalThis.String(object.providerId)
        : isSet(object.provider_id)
        ? globalThis.String(object.provider_id)
        : "",
      editionId: isSet(object.editionId)
        ? globalThis.String(object.editionId)
        : isSet(object.edition_id)
        ? globalThis.String(object.edition_id)
        : "",
    };
  },

  toJSON(message: GetChallengeRequest): unknown {
    const obj: any = {};
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.editionId !== "") {
      obj.editionId = message.editionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetChallengeRequest>, I>>(base?: I): GetChallengeRequest {
    return GetChallengeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetChallengeRequest>, I>>(object: I): GetChallengeRequest {
    const message = createBaseGetChallengeRequest();
    message.providerId = object.providerId ?? "";
    message.editionId = object.editionId ?? "";
    return message;
  },
};

function createBaseGetChallengeResponse(): GetChallengeResponse {
  return { challengePayload: "" };
}

export const GetChallengeResponse: MessageFns<GetChallengeResponse> = {
  encode(message: GetChallengeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.challengePayload !== "") {
      writer.uint32(10).string(message.challengePayload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetChallengeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetChallengeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.challengePayload = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetChallengeResponse {
    return {
      challengePayload: isSet(object.challengePayload)
        ? globalThis.String(object.challengePayload)
        : isSet(object.challenge_payload)
        ? globalThis.String(object.challenge_payload)
        : "",
    };
  },

  toJSON(message: GetChallengeResponse): unknown {
    const obj: any = {};
    if (message.challengePayload !== "") {
      obj.challengePayload = message.challengePayload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetChallengeResponse>, I>>(base?: I): GetChallengeResponse {
    return GetChallengeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetChallengeResponse>, I>>(object: I): GetChallengeResponse {
    const message = createBaseGetChallengeResponse();
    message.challengePayload = object.challengePayload ?? "";
    return message;
  },
};

function createBaseVerifyRequest(): VerifyRequest {
  return { providerId: "", clientPayload: "" };
}

export const VerifyRequest: MessageFns<VerifyRequest> = {
  encode(message: VerifyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.providerId !== "") {
      writer.uint32(10).string(message.providerId);
    }
    if (message.clientPayload !== "") {
      writer.uint32(18).string(message.clientPayload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientPayload = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyRequest {
    return {
      providerId: isSet(object.providerId)
        ? globalThis.String(object.providerId)
        : isSet(object.provider_id)
        ? globalThis.String(object.provider_id)
        : "",
      clientPayload: isSet(object.clientPayload)
        ? globalThis.String(object.clientPayload)
        : isSet(object.client_payload)
        ? globalThis.String(object.client_payload)
        : "",
    };
  },

  toJSON(message: VerifyRequest): unknown {
    const obj: any = {};
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.clientPayload !== "") {
      obj.clientPayload = message.clientPayload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyRequest>, I>>(base?: I): VerifyRequest {
    return VerifyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyRequest>, I>>(object: I): VerifyRequest {
    const message = createBaseVerifyRequest();
    message.providerId = object.providerId ?? "";
    message.clientPayload = object.clientPayload ?? "";
    return message;
  },
};

function createBaseVerifyResponse(): VerifyResponse {
  return { success: false, voucher: "" };
}

export const VerifyResponse: MessageFns<VerifyResponse> = {
  encode(message: VerifyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.voucher !== "") {
      writer.uint32(18).string(message.voucher);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.voucher = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      voucher: isSet(object.voucher) ? globalThis.String(object.voucher) : "",
    };
  },

  toJSON(message: VerifyResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.voucher !== "") {
      obj.voucher = message.voucher;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyResponse>, I>>(base?: I): VerifyResponse {
    return VerifyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyResponse>, I>>(object: I): VerifyResponse {
    const message = createBaseVerifyResponse();
    message.success = object.success ?? false;
    message.voucher = object.voucher ?? "";
    return message;
  },
};

/** / Handles captcha and bot protection routines across multiple verification providers. */
export interface HumanVerificationService {
  /**
   * / Asks the backend to generate a new verification challenge.
   * / For standard cloud providers (Turnstile/reCAPTCHA), this may return an empty payload.
   */
  GetChallenge(request: GetChallengeRequest): Promise<GetChallengeResponse>;
  /** / Checks the user's submitted answer or token to see if they passed the challenge. */
  Verify(request: VerifyRequest): Promise<VerifyResponse>;
}

export const HumanVerificationServiceServiceName = "human_verification.v1.HumanVerificationService";
export class HumanVerificationServiceClientImpl implements HumanVerificationService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || HumanVerificationServiceServiceName;
    this.rpc = rpc;
    this.GetChallenge = this.GetChallenge.bind(this);
    this.Verify = this.Verify.bind(this);
  }
  GetChallenge(request: GetChallengeRequest): Promise<GetChallengeResponse> {
    const data = GetChallengeRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetChallenge", data);
    return promise.then((data) => GetChallengeResponse.decode(new BinaryReader(data)));
  }

  Verify(request: VerifyRequest): Promise<VerifyResponse> {
    const data = VerifyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Verify", data);
    return promise.then((data) => VerifyResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
