// @ts-nocheck

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: email/v1/email.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "email.v1";

/** / Request details to fetch a user's email state. */
export interface GetEmailRequest {
  /** / Unique identifier of the user account. */
  userId: string;
}

/** / Current and staged email data for the account. */
export interface GetEmailResponse {
  /** / The actively verified email address currently receiving notifications. */
  currentEmail: string;
  /** / True if the current email address has completed ownership verification. */
  isVerified: boolean;
  /** / A staged email address awaiting verification before replacing the current email. */
  pendingNewEmail: string;
  /** / The strategy used to validate ownership (e.g., OnetimeCode, MagicLink). */
  verificationType: string;
}

/** / Request details to change or set a user's email. */
export interface SetEmailRequest {
  /** / Unique identifier of the user account. */
  userId: string;
  /** / The new email address targeted for association. */
  newEmail: string;
}

/** / Result of initiating an email change. */
export interface SetEmailResponse {
  /** / Internal tracking state of the change request (e.g., PendingVerification, Throttled). */
  status: string;
}

/** / Submission details to complete verification. */
export interface VerifyEmailRequest {
  /** / Unique identifier of the user account. */
  userId: string;
  /** / The short-lived numeric or alphanumeric token sent to the pending email. */
  code: string;
}

/** / The outcome of the verification attempt. */
export interface VerifyEmailResponse {
  /** / True if the code matched and the verification process succeeded. */
  success: boolean;
  /** / If the verification finalized a multi-step email change, this contains the new active email. */
  emailUpdatedTo: string;
}

function createBaseGetEmailRequest(): GetEmailRequest {
  return { userId: "" };
}

export const GetEmailRequest: MessageFns<GetEmailRequest> = {
  encode(message: GetEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEmailRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
    };
  },

  toJSON(message: GetEmailRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEmailRequest>, I>>(base?: I): GetEmailRequest {
    return GetEmailRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEmailRequest>, I>>(object: I): GetEmailRequest {
    const message = createBaseGetEmailRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetEmailResponse(): GetEmailResponse {
  return { currentEmail: "", isVerified: false, pendingNewEmail: "", verificationType: "" };
}

export const GetEmailResponse: MessageFns<GetEmailResponse> = {
  encode(message: GetEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.currentEmail !== "") {
      writer.uint32(10).string(message.currentEmail);
    }
    if (message.isVerified !== false) {
      writer.uint32(16).bool(message.isVerified);
    }
    if (message.pendingNewEmail !== "") {
      writer.uint32(26).string(message.pendingNewEmail);
    }
    if (message.verificationType !== "") {
      writer.uint32(34).string(message.verificationType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.currentEmail = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isVerified = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pendingNewEmail = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.verificationType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEmailResponse {
    return {
      currentEmail: isSet(object.currentEmail)
        ? globalThis.String(object.currentEmail)
        : isSet(object.current_email)
        ? globalThis.String(object.current_email)
        : "",
      isVerified: isSet(object.isVerified)
        ? globalThis.Boolean(object.isVerified)
        : isSet(object.is_verified)
        ? globalThis.Boolean(object.is_verified)
        : false,
      pendingNewEmail: isSet(object.pendingNewEmail)
        ? globalThis.String(object.pendingNewEmail)
        : isSet(object.pending_new_email)
        ? globalThis.String(object.pending_new_email)
        : "",
      verificationType: isSet(object.verificationType)
        ? globalThis.String(object.verificationType)
        : isSet(object.verification_type)
        ? globalThis.String(object.verification_type)
        : "",
    };
  },

  toJSON(message: GetEmailResponse): unknown {
    const obj: any = {};
    if (message.currentEmail !== "") {
      obj.currentEmail = message.currentEmail;
    }
    if (message.isVerified !== false) {
      obj.isVerified = message.isVerified;
    }
    if (message.pendingNewEmail !== "") {
      obj.pendingNewEmail = message.pendingNewEmail;
    }
    if (message.verificationType !== "") {
      obj.verificationType = message.verificationType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEmailResponse>, I>>(base?: I): GetEmailResponse {
    return GetEmailResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEmailResponse>, I>>(object: I): GetEmailResponse {
    const message = createBaseGetEmailResponse();
    message.currentEmail = object.currentEmail ?? "";
    message.isVerified = object.isVerified ?? false;
    message.pendingNewEmail = object.pendingNewEmail ?? "";
    message.verificationType = object.verificationType ?? "";
    return message;
  },
};

function createBaseSetEmailRequest(): SetEmailRequest {
  return { userId: "", newEmail: "" };
}

export const SetEmailRequest: MessageFns<SetEmailRequest> = {
  encode(message: SetEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.newEmail !== "") {
      writer.uint32(18).string(message.newEmail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newEmail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetEmailRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      newEmail: isSet(object.newEmail)
        ? globalThis.String(object.newEmail)
        : isSet(object.new_email)
        ? globalThis.String(object.new_email)
        : "",
    };
  },

  toJSON(message: SetEmailRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.newEmail !== "") {
      obj.newEmail = message.newEmail;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetEmailRequest>, I>>(base?: I): SetEmailRequest {
    return SetEmailRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetEmailRequest>, I>>(object: I): SetEmailRequest {
    const message = createBaseSetEmailRequest();
    message.userId = object.userId ?? "";
    message.newEmail = object.newEmail ?? "";
    return message;
  },
};

function createBaseSetEmailResponse(): SetEmailResponse {
  return { status: "" };
}

export const SetEmailResponse: MessageFns<SetEmailResponse> = {
  encode(message: SetEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetEmailResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: SetEmailResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetEmailResponse>, I>>(base?: I): SetEmailResponse {
    return SetEmailResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetEmailResponse>, I>>(object: I): SetEmailResponse {
    const message = createBaseSetEmailResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseVerifyEmailRequest(): VerifyEmailRequest {
  return { userId: "", code: "" };
}

export const VerifyEmailRequest: MessageFns<VerifyEmailRequest> = {
  encode(message: VerifyEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyEmailRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      code: isSet(object.code) ? globalThis.String(object.code) : "",
    };
  },

  toJSON(message: VerifyEmailRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyEmailRequest>, I>>(base?: I): VerifyEmailRequest {
    return VerifyEmailRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyEmailRequest>, I>>(object: I): VerifyEmailRequest {
    const message = createBaseVerifyEmailRequest();
    message.userId = object.userId ?? "";
    message.code = object.code ?? "";
    return message;
  },
};

function createBaseVerifyEmailResponse(): VerifyEmailResponse {
  return { success: false, emailUpdatedTo: "" };
}

export const VerifyEmailResponse: MessageFns<VerifyEmailResponse> = {
  encode(message: VerifyEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.emailUpdatedTo !== "") {
      writer.uint32(18).string(message.emailUpdatedTo);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.emailUpdatedTo = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyEmailResponse {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      emailUpdatedTo: isSet(object.emailUpdatedTo)
        ? globalThis.String(object.emailUpdatedTo)
        : isSet(object.email_updated_to)
        ? globalThis.String(object.email_updated_to)
        : "",
    };
  },

  toJSON(message: VerifyEmailResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.emailUpdatedTo !== "") {
      obj.emailUpdatedTo = message.emailUpdatedTo;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerifyEmailResponse>, I>>(base?: I): VerifyEmailResponse {
    return VerifyEmailResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerifyEmailResponse>, I>>(object: I): VerifyEmailResponse {
    const message = createBaseVerifyEmailResponse();
    message.success = object.success ?? false;
    message.emailUpdatedTo = object.emailUpdatedTo ?? "";
    return message;
  },
};

/** / Manages email affiliations, verification lifecycles, and ownership transfers for accounts. */
export interface EmailService {
  /** / Retrieves the current and pending email states for a user. */
  GetEmail(request: GetEmailRequest): Promise<GetEmailResponse>;
  /** / Initiates or updates an email change request, staging it for verification. */
  SetEmail(request: SetEmailRequest): Promise<SetEmailResponse>;
  /** / Validates a verification code to finalize an email update. */
  VerifyEmail(request: VerifyEmailRequest): Promise<VerifyEmailResponse>;
}

export const EmailServiceServiceName = "email.v1.EmailService";
export class EmailServiceClientImpl implements EmailService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || EmailServiceServiceName;
    this.rpc = rpc;
    this.GetEmail = this.GetEmail.bind(this);
    this.SetEmail = this.SetEmail.bind(this);
    this.VerifyEmail = this.VerifyEmail.bind(this);
  }
  GetEmail(request: GetEmailRequest): Promise<GetEmailResponse> {
    const data = GetEmailRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetEmail", data);
    return promise.then((data) => GetEmailResponse.decode(new BinaryReader(data)));
  }

  SetEmail(request: SetEmailRequest): Promise<SetEmailResponse> {
    const data = SetEmailRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "SetEmail", data);
    return promise.then((data) => SetEmailResponse.decode(new BinaryReader(data)));
  }

  VerifyEmail(request: VerifyEmailRequest): Promise<VerifyEmailResponse> {
    const data = VerifyEmailRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "VerifyEmail", data);
    return promise.then((data) => VerifyEmailResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
