// @ts-nocheck

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.0
// source: access_tokens/v1/access_tokens.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "access_tokens.v1";

export interface IssueTokenRequest {
  /** / Opaque stateful session token used as the primary credential. */
  sessionToken: string;
  /** / Requested permission scopes or roles (e.g., ["chat:read", "chat:write"]). */
  roles: string[];
  /** / Requested token lifetime in seconds, bounded by server maximum policy. */
  ttlSeconds: number;
}

export interface IssueTokenResponse {
  /** / Signed JWT access token containing embedded claims and roles. */
  accessToken: string;
  /** / Unix epoch timestamp in seconds indicating token expiration. */
  expiresAt: Long;
}

export interface GetPublicKeyRequest {
}

export interface GetPublicKeyResponse {
  /** / Public key in PEM format used for asymmetric signature verification. */
  publicKeyPem: string;
}

function createBaseIssueTokenRequest(): IssueTokenRequest {
  return { sessionToken: "", roles: [], ttlSeconds: 0 };
}

export const IssueTokenRequest: MessageFns<IssueTokenRequest> = {
  encode(message: IssueTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionToken !== "") {
      writer.uint32(10).string(message.sessionToken);
    }
    for (const v of message.roles) {
      writer.uint32(18).string(v!);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(24).uint32(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssueTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssueTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ttlSeconds = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssueTokenRequest {
    return {
      sessionToken: isSet(object.sessionToken)
        ? globalThis.String(object.sessionToken)
        : isSet(object.session_token)
        ? globalThis.String(object.session_token)
        : "",
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
      ttlSeconds: isSet(object.ttlSeconds)
        ? globalThis.Number(object.ttlSeconds)
        : isSet(object.ttl_seconds)
        ? globalThis.Number(object.ttl_seconds)
        : 0,
    };
  },

  toJSON(message: IssueTokenRequest): unknown {
    const obj: any = {};
    if (message.sessionToken !== "") {
      obj.sessionToken = message.sessionToken;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IssueTokenRequest>, I>>(base?: I): IssueTokenRequest {
    return IssueTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IssueTokenRequest>, I>>(object: I): IssueTokenRequest {
    const message = createBaseIssueTokenRequest();
    message.sessionToken = object.sessionToken ?? "";
    message.roles = object.roles?.map((e) => e) || [];
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseIssueTokenResponse(): IssueTokenResponse {
  return { accessToken: "", expiresAt: Long.UZERO };
}

export const IssueTokenResponse: MessageFns<IssueTokenResponse> = {
  encode(message: IssueTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    if (!message.expiresAt.equals(Long.UZERO)) {
      writer.uint32(16).uint64(message.expiresAt.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssueTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssueTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expiresAt = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssueTokenResponse {
    return {
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      expiresAt: isSet(object.expiresAt)
        ? Long.fromValue(object.expiresAt)
        : isSet(object.expires_at)
        ? Long.fromValue(object.expires_at)
        : Long.UZERO,
    };
  },

  toJSON(message: IssueTokenResponse): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (!message.expiresAt.equals(Long.UZERO)) {
      obj.expiresAt = (message.expiresAt || Long.UZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IssueTokenResponse>, I>>(base?: I): IssueTokenResponse {
    return IssueTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IssueTokenResponse>, I>>(object: I): IssueTokenResponse {
    const message = createBaseIssueTokenResponse();
    message.accessToken = object.accessToken ?? "";
    message.expiresAt = (object.expiresAt !== undefined && object.expiresAt !== null)
      ? Long.fromValue(object.expiresAt)
      : Long.UZERO;
    return message;
  },
};

function createBaseGetPublicKeyRequest(): GetPublicKeyRequest {
  return {};
}

export const GetPublicKeyRequest: MessageFns<GetPublicKeyRequest> = {
  encode(_: GetPublicKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPublicKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPublicKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetPublicKeyRequest {
    return {};
  },

  toJSON(_: GetPublicKeyRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPublicKeyRequest>, I>>(base?: I): GetPublicKeyRequest {
    return GetPublicKeyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPublicKeyRequest>, I>>(_: I): GetPublicKeyRequest {
    const message = createBaseGetPublicKeyRequest();
    return message;
  },
};

function createBaseGetPublicKeyResponse(): GetPublicKeyResponse {
  return { publicKeyPem: "" };
}

export const GetPublicKeyResponse: MessageFns<GetPublicKeyResponse> = {
  encode(message: GetPublicKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKeyPem !== "") {
      writer.uint32(10).string(message.publicKeyPem);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPublicKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPublicKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publicKeyPem = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPublicKeyResponse {
    return {
      publicKeyPem: isSet(object.publicKeyPem)
        ? globalThis.String(object.publicKeyPem)
        : isSet(object.public_key_pem)
        ? globalThis.String(object.public_key_pem)
        : "",
    };
  },

  toJSON(message: GetPublicKeyResponse): unknown {
    const obj: any = {};
    if (message.publicKeyPem !== "") {
      obj.publicKeyPem = message.publicKeyPem;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPublicKeyResponse>, I>>(base?: I): GetPublicKeyResponse {
    return GetPublicKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPublicKeyResponse>, I>>(object: I): GetPublicKeyResponse {
    const message = createBaseGetPublicKeyResponse();
    message.publicKeyPem = object.publicKeyPem ?? "";
    return message;
  },
};

/**
 * ============================================================================
 * Service: AccessTokensService
 * Description:
 *   Stateless token issuance authority that converts stateful session tokens
 *   into short-lived, cryptographically signed JSON Web Tokens (JWTs).
 * ============================================================================
 */
export interface AccessTokensService {
  /** / Validates a session token and issues a short-lived scoped JWT access token. */
  IssueToken(request: IssueTokenRequest): Promise<IssueTokenResponse>;
  /** / Exposes the public cryptographic key for offline token signature verification. */
  GetPublicKey(request: GetPublicKeyRequest): Promise<GetPublicKeyResponse>;
}

export const AccessTokensServiceServiceName = "access_tokens.v1.AccessTokensService";
export class AccessTokensServiceClientImpl implements AccessTokensService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AccessTokensServiceServiceName;
    this.rpc = rpc;
    this.IssueToken = this.IssueToken.bind(this);
    this.GetPublicKey = this.GetPublicKey.bind(this);
  }
  IssueToken(request: IssueTokenRequest): Promise<IssueTokenResponse> {
    const data = IssueTokenRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "IssueToken", data);
    return promise.then((data) => IssueTokenResponse.decode(new BinaryReader(data)));
  }

  GetPublicKey(request: GetPublicKeyRequest): Promise<GetPublicKeyResponse> {
    const data = GetPublicKeyRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetPublicKey", data);
    return promise.then((data) => GetPublicKeyResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
