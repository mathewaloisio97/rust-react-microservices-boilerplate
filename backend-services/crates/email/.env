# -----------------------------------------------------------------------------
# COMPILE-TIME ONLY: This file is exclusively used by the `sqlx` macro during 
# `cargo build` or `cargo check` to verify SQL queries against the live schema.
# At runtime, the service looks for EMAIL_DATABASE_URL instead.
# -----------------------------------------------------------------------------
DATABASE_URL=postgres://postgres@localhost:5432/your_app_email
